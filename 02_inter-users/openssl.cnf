[ default ]
name            = inter-users-ca
domain_suffix   = grupo6.local
aia_url         = http://$name.$domain_suffix/$name.crt
crl_url         = http://$name.$domain_suffix/$name.crl
default_ca      = CA_default
name_opt        = ca_default
cert_opt        = ca_default

[ CA_default ]
dir             = .
certs           = $dir/certs
crl_dir         = $dir/crl
new_certs_dir   = $dir/newcerts
database        = $dir/db/index.txt
serial          = $dir/db/serial
crlnumber       = $dir/db/crlnumber
rand_serial     = yes

# Chaves DESTA Intermedia
private_key     = $dir/private/inter-users.key
certificate     = $dir/certs/inter-users.crt

default_days    = 365
default_crl_days= 30
default_md      = sha256
preserve        = no
policy          = policy_loose

[ policy_loose ]
# Politica mais relaxada para utilizadores
countryName             = optional
stateOrProvinceName     = optional
organizationName        = optional
organizationalUnitName  = optional
commonName              = supplied
emailAddress            = optional

[ req ]
default_bits        = 4096
distinguished_name  = req_distinguished_name
string_mask         = utf8only
default_md          = sha256
x509_extensions     = v3_intermediate_ca

[ req_distinguished_name ]
countryName                     = Country Name (2 letter code)
stateOrProvinceName             = State or Province Name
organizationName                = Organization Name
organizationalUnitName          = Organizational Unit Name
commonName                      = Common Name
emailAddress                    = Email Address

countryName_default             = PT
stateOrProvinceName_default     = Porto
organizationName_default        = ISEP
organizationalUnitName_default  = HR Dept
commonName_default              = Grupo6 Users CA
emailAddress_default            = admin@grupo6.com

[ v3_intermediate_ca ]
# Extensoes para o pedido DESTA CA (que vai para a Root)
subjectKeyIdentifier = hash
authorityKeyIdentifier = keyid:always,issuer
basicConstraints = critical, CA:true, pathlen:0
keyUsage = critical, digitalSignature, cRLSign, keyCertSign

[ usr_cert ]
# Extensoes para quando ESTA CA assinar PESSOAS
basicConstraints = CA:FALSE
nsCertType = client, email
keyUsage = critical, nonRepudiation, digitalSignature, keyEncipherment
extendedKeyUsage = clientAuth, emailProtection
authorityKeyIdentifier = keyid,issuer
